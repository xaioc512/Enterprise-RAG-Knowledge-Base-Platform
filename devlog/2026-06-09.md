# 开发日志 - 2026年06月09日

## 完成事项

### 部门管理与权限控制系统

#### 数据库
- 新增 `departments` 表（name, description）
- `users` 表新增 `department_id` FK
- `documents` 表新增 `department_id` + `visibility` (public/department/restricted)
- 新增 `document_departments` 多对多关联表
- Alembic 迁移: `6f8a2c3d9e4b`
- 预置 5 个默认部门：技术部、财务部、人事部、市场部、综合管理部

#### 后端 (8个文件修改/新建)
- **Model**: `models/department.py` 新建，`user.py`/`document.py` 修改
- **Schema**: `schemas/department.py` 新建，`user.py`/`document.py`/`auth.py` 修改（添加 department_id 字段）
- **API**: `api/departments.py` 新建 (CRUD)
- **认证**: `auth.py`/`auth_service.py` 支持 department_id 写入 JWT
- **用户**: `users.py` 创建/更新用户支持 department_id
- **文档**: `documents.py` 上传支持 visibility + department_id；列表/详情按权限过滤；普通用户 403 拒绝删除
- **RAG**: `retriever.py`/`chat.py` 检索时按用户部门权限过滤 Chroma 结果

#### 前端 (5个文件修改/新建)
- **新建** `views/DepartmentManage.vue` — 部门 CRUD 卡片页
- **更新** `views/DocumentManage.vue` — 上传时选可见范围(公开/部门/受限)；卡片显示权限标签；**普通用户隐藏删除按钮**
- **更新** `views/UserManage.vue` — 创建用户时选部门；卡片显示部门
- **更新** `views/Register.vue` — 注册时可选部门
- **更新** `views/CategoryManage.vue` — 侧边栏加部门管理入口
- **更新** `router/index.js` — 新增 /admin/departments 路由；文档管理对普通用户开放(只读)
- **更新** `stores/auth.js` — 添加 departmentId getter, register 支持 department_id

### 权限矩阵

| 操作 | Admin | 部门用户 |
|------|-------|---------|
| 查看文档列表 | 全部 | public + 本部门department + restricted共享给本部门的 |
| 预览文档 | 全部 | 同上 |
| 上传文档 | ✅ | ❌ |
| 删除文档 | ✅ | ❌ (403) |
| AI问答(RAG检索) | 检索全部 | 仅检索有权访问的文档 |
| 管理部门 | ✅ | ❌ |
| 管理用户 | ✅ | ❌ |

### 全链路验证测试 ✅
- Admin 看到全部 4 个文档
- 技术部用户看到：技术部门文档 + 3个公开文档 = 4个
- 财务部用户看到：仅 3 个公开文档（看不到技术部文档）
- 普通用户 DELETE 文档返回 403
- 前端 build 成功

## 待办事项
- 后续可优化：RAG 检索异步化（避免 Chroma sync 阻塞）
- 后续可扩展：部门层级（parent_id）、部门管理员角色
